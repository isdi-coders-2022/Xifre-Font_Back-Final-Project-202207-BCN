export const routers = {
  users: "/users",
  projects: "/projects",
};

export const endpoints = {
  logIn: "/log-in",
  signUp: "/sign-up",
  getUserData: "/:userId",

  getAllProjects: "/all",
  projectById: "/:projectId",
  createProject: "/new",
};
